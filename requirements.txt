# Simple-modelling track dependencies (Python 3.11)
# No FinRL / DeepAR / GluonTS — those belong to the old v0.17 stack on other branches.

pandas>=2.0
numpy>=1.24
scipy>=1.10
stable-baselines3>=2.0
gymnasium>=0.29
yfinance>=1.2
torch>=2.0
matplotlib>=3.7
tqdm>=4.65

# Phase 0 optional games (install separately if needed):
#   pip install flappy-bird-gymnasium "gymnasium[box2d]"
