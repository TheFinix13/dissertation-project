# Outstanding sections — tracking to final submission

Tracked so nothing is forgotten before final submission. The Word documents
are the source of truth; the PDF build mirrors them except where noted.

## Done (author's own text now in the build)

- ~~Chapter 6~~ DONE 31 Aug — Fiyin's version in `Full Report - 5.docx`,
  split out by `build.py` into `ch6.tex`.
- ~~Chapter 1~~ DONE 1 Sep — Fiyin's version in `Full report 1.docx`,
  converted to `ch1_new.tex` and built into `ch1.tex`. The agent draft is
  kept at `ch1_agent_draft_backup.tex` for reference only.
- ~~Section 6.4.2 Monthly Episode Structure~~ DONE 1 Sep — new limitation
  written in `Full report 1.docx`; `build.py` splices it into Chapter 6
  before "Fixed Trade Size", so the PDF numbers it 6.4.2 and realigns
  6.4.3–6.4.6 automatically.
- ~~Section 5.2 Experiment Protocols and Evaluation Criteria~~ DONE 1 Sep —
  Fiyin's rewrite in `Full report 1.docx` replaces the old placeholders;
  the agent filler (`fill_sec52_placeholders`) has been deleted from
  `build.py`.
- ~~Section 4.4.3 Benchmark Implementation~~ DONE 1 Sep — Fiyin's rewrite
  in `Full report 1.docx` replaces the old incomplete section.

Note: the rewritten 5.2 / 4.4.3 / 6.4.2 live in `Full report 1.docx`, not
in reports 4 and 5. If those blocks are ever moved into their home
documents, update `split_ch1_fragment` in `build.py` accordingly.

## Small fixes — DONE 7 Sep in the LaTeX (Word docs still carry the old text)

- ~~Section 6.1 "test set (2025-2025)"~~ → "(2024-2025)" fixed in ch6.tex.
- ~~"12 implementation problems" in 6.3~~ → now "several implementation
  problems ... three of which could have affected the experiments'
  validity", matching Chapter 4. Fixed in ch6.tex (the unused ch5_part2
  intermediate was deleted in the restructure).
- Also fixed in ch6.tex: "Each stem" → "Each stems", "ignoring their
  market observations" → "ignoring its", "the Trade size" → "the trade
  size".

## Done 1 Sep — front/back matter and format (all in main_full.tex)

- Front cover with ©2026 line, supervisor, URN. **Check supervisor's full
  name/title** — currently "Mr. Nguyen".
- Statement of Originality page (standard Surrey wording).
- Acknowledgements (short draft — Fiyin should personalise).
- List of Figures, List of Tables (build.py registers the author's
  "Table N.M:" captions via add_table_lot_entries).
- ~~List of Abbreviations and Symbols~~ removed 1 Sep at Fiyin's request.
- Appendices: A verification tests (21), B per-month held-out results
  (generated by make_appendix_permonth.py from final_results.json),
  C glossary of terms.
- Format switched to handbook spec: Times New Roman 11pt, 1.5 spacing,
  justified, serial page numbers, 12pt bold sub-headings.

## Done 1 Sep — abstract, Chapter 2, citations, references

- Abstract in main_full.tex front matter (after Statement of Originality).
  Numbers sourced from Chapters 5–6; Fiyin should read and personalise.
- Chapter 2 written fresh in `ch2.tex` (agent draft — Fiyin to review).
  Five sections: RL for sequential decisions, the two algorithm families,
  RL for trading (incl. close reading of Théate & Ernst's SPY result),
  honest evaluation, gap statement tied to the four research questions.
- IEEE citations: `references.bib` (all canonical, verifiable entries;
  added Henderson 2018 seeds paper and Huang & Ontañón 2022 masking paper).
  `build.py` now has a CITATIONS map inserting \cite at 20 anchor points
  across ch1/ch3/ch4/ch5/ch6; it warns if a Word-doc edit moves an anchor.
  Both main.tex and main_full.tex run bibtex with IEEEtran style.
- ∑ now renders as the big summation operator (\Umathchardef fix in both
  preambles) instead of a small letter sigma.

## Done 1 Sep — author's front matter (from Full report 1.docx)

- Statement of Originality, Acknowledgements, and Abstract replaced with
  Fiyin's own wording (appended at the end of `Full report 1.docx`).
  Text lives statically in main_full.tex; `split_ch1_fragment` in
  build.py now cuts Section 4.4.3 before these blocks so they cannot
  leak into Chapter 4.
- Cover updated: "Mr. Nguyen" → "Dr. Nguyen" (matches acknowledgements).

## Done 1 Sep — Chapter 2 final rewrite (post-audit)

Applied the edits agreed across two adversarial audits to ch2.tex:
- Cut AlphaGo (silver2016 now uncited; bibtex drops it automatically).
- End of 2.3: new synthesis paragraph mapping the corpus (asset universe,
  actions, rewards, costs, benchmarks, seeds, behaviour) to specific
  citations, plus the selection-criterion sentence.
- Théate & Ernst: added the "consistent with either state use or a fixed
  schedule" boundary sentence.
- Moody paragraph: information-vs-incentive framing for RQ3/RQ4, with
  causal-modesty sentence ("do not establish why").
- 2.4: "strawman" claim reframed as capitalisation/seriousness criterion.
- EMH paragraph: "this study operationalises that null as buy-and-hold".
- 2.5 rewritten in three paragraphs: (1) performance vs state dependence
  as separate evidence dimensions, (2) attribution vs sensitivity vs
  mask-conditioned diagnostic with the cash/holdings confound spelled
  out and an explicit definition of "state dependence", (3) explicit
  not-claiming list and the contribution as controlled combination +
  diagnostic as reported outcome.
Recompiled clean: 119 pages, no undefined citations.

## Done 7 Sep — author's Chapter 2 (from Full report 1.docx)

ch2.tex regenerated from Fiyin's own rewrite (docx moved to
`Dissertation Drafts/Full Report Draft/Full report 1.docx` — note new
subfolder if build.py paths are ever re-run). Conversion details:
- 49 plain-text [n] citations mapped to \cite keys using the IEEEtran
  first-use order from main_full.bbl; numbering verified unchanged
  ([22] Théate, [23] Guan render correctly).
- Headings 2.x → \section, 2.x.y → \subsection; banner stripped.
- Four typo fixes applied ("realistic capital"→"realistic alternative",
  "evaluated them ... once ,without"→"evaluated once ... , without",
  dropped double connector "For this reason ... therefore",
  "Related"→"related"). Two dash normalisations in the intro.
- Section 2.5 title is now "The Gap Addressed In This Study".
Compiled clean: 123 pages (was 119), no undefined citations.

## Done 7 Sep — style registration + full citation pass

- Fiyin's voice fingerprint written to brain-box:
  `school/methodology/fiyin-writing-style.md` (assembly pattern, 14 voice
  rules, known slips). Workspace rule `brain-box-writing.mdc` now lists it
  first — future report prose gets written in his voice directly.
- Citation audit across all chapters. Added six anchors (also mirrored in
  build.py CITATIONS so rebuilds reproduce them):
  ch4: seeds/variation → henderson2018.
  ch5: six-seeds rationale → henderson2018; fee response mirrors
  Théate & Ernst → theate2021; benchmark difficulty consistent with
  market efficiency → fama1970.
  ch6: Sharpe-based future objective → moody2001; missing period fixed.
- Coverage now: ch1 4 · ch2 45 · ch3 10 · ch4 5 · ch5 6 · ch6 2 cite
  points. All render with correct IEEE numbers; no undefined citations;
  123 pages.

## Done 7 Sep — flow fixes + page reduction (104 → 87 content pages)

IMPORTANT: build.py is now FROZEN (guard added, needs --force). The
chapter .tex files contain manual edits that a rebuild would destroy.
Compile with: xelatex main_full → bibtex main_full → xelatex ×2.

Flow/hindsight fixes (first-time-reader audit):
- ch3 trade size no longer hardcoded 0.1 C0: now Δ = f·C0 with f selected
  on validation (Section 5.3). Fixed in notation table + §3.4.
- ch3 §3.2.3 wrong split "58 train / 38 test" → 60/12/24 with validation.
- ch3 notation table said s_t ∈ ℝ⁸ "eight features" → ℝ⁹ nine.
- Algorithm 2 dangling "(3.16)" ref → "(Section 3.8.5)".
- ch1 March-2025 example now framed "Under the experimental setup used
  later in this study".
- ch3 c_t,v_{t-1} typo → c_t + v_t; duplicated reward-to-go sentence in
  §3.6.1 removed; ch4 triple-stated 43.1% fact reduced to once.

Page reduction (content pages = ch1 start to bibliography):
- ch3 45 → 32 pp: stages 1–4 prose folded into one passage (Table 3.4
  ladder carries the narrative); reward-measurability derivation deferred
  to §3.5; three POMDP/limitations passages merged; Table 3.3a dropped
  (3.3b renamed 3.3); Trading Actions + Fixed Trading Amount merged;
  §3.4.6 and §3.6.3 compressed; §3.9.5 comparison table dropped; ~40
  one-token display equations inlined; algorithm boxes moved to new
  Appendix B (Algorithm Listings), pointer left in §3.10 area.
- ch4 12 → 11 pp: split longtable dropped (figure + prose remain),
  arrow-chain pipeline summary dropped (Figure 4.2 shows it), Appendix A
  pointer added to §4.5.
- ch5 §5.9 compressed: restated numbers from 5.3/5.8 reduced to
  references; 5.9.3 tightened.
- Result: 110 total pages; content 87 (ch1 5 · ch2 10 · ch3 32 · ch4 11 ·
  ch5 22 · ch6 7); front matter 11; bib+appendices 12.

## Done 7 Sep — appendix voice rewrite, external review, restructure

- All four appendices rewritten in Fiyin's voice (A verification tests,
  B algorithm listings, C per-month results, D glossary). Stale
  ladder/rung and "pointwise recoverable" phrasing replaced; the
  no-trades accounting check no longer presented as a benchmark.
- External-reviewer self-grade against the handbook: technical 71 (80%),
  report quality 74 (20%) → overall ≈72, Excellent band. Highest-value
  improvements identified: bootstrap CIs over existing per-month/per-seed
  results; one simple active baseline (e.g. moving-average) on the same
  test months.
- Workspace restructured; repo now single `main` branch with era tags.
  This folder renamed latex/supervisor_preview → latex/dissertation.
  Full pre-cleanup state preserved under tag era3-pre-restructure-snapshot.

## Known remaining work

1. Optional: 6.3 paragraph reflecting on planning/time management
   (drafted in chat 1 Sep, not yet inserted). The grade descriptors
   explicitly mention "planning and organisation of the project".
2. Declaration of originality form at submission (separate form).
3. Page budget: content at 87 vs the ~80 target. Remaining honest levers
   are small (figure widths save <0.5 pp total). Getting under 84 would
   require thinning the REINFORCE/DQN derivations or user-written text.
4. Optional mark-chasers from the external review: bootstrap CIs
   (~1 day, no retraining) and a moving-average baseline (~half day).
5. Viva pack: 12 slides + demo not started.
6. Submission comment on page count drafted in chat 7 Sep (110 total =
   87 content + front matter + bib/appendices).

## Build order (now with bibliography)

xelatex main_full → bibtex main_full → xelatex ×2. build.py is frozen
(--force to regenerate chapters, which would DESTROY the manual trims).
